/* tslint:disable */
/* eslint-disable */
/**
 * supports a RESTful API for the Libpod library
 * This documentation describes the Podman v2.x+ RESTful API. It consists of a Docker-compatible API and a Libpod API providing support for Podman’s unique features such as pods.  To start the service and keep it running for 5,000 seconds (-t 0 runs forever):  podman system service -t 5000 &  You can then use cURL on the socket using requests documented below.  NOTE: if you install the package podman-docker, it will create a symbolic link for /run/docker.sock to /run/podman/podman.sock  NOTE: Some fields in the API response JSON are encoded as omitempty, which means that if said field has a zero value, they will not be encoded in the API response. This is a feature to help reduce the size of the JSON responses returned via the API.  NOTE: Due to the limitations of [go-swagger](https://github.com/go-swagger/go-swagger), some field values that have a complex type show up as null in the docs as well as in the API responses. This is because the zero value for the field type is null. The field description in the docs will state what type the field is expected to be for such cases.  See podman-system-service(1) for more information.  Quick Examples:  \'podman info\'  curl --unix-socket /run/podman/podman.sock http://d/v5.0.0/libpod/info  \'podman pull quay.io/containers/podman\'  curl -XPOST --unix-socket /run/podman/podman.sock -v \'http://d/v5.0.0/images/create?fromImage=quay.io%2Fcontainers%2Fpodman\'  \'podman list images\'  curl --unix-socket /run/podman/podman.sock -v \'http://d/v5.0.0/libpod/images/json\' | jq
 *
 * The version of the OpenAPI document: 5.0.0
 * Contact: podman@lists.podman.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { Subnet } from './Subnet';
import {
    SubnetFromJSON,
    SubnetFromJSONTyped,
    SubnetToJSON,
} from './Subnet';
import type { Route } from './Route';
import {
    RouteFromJSON,
    RouteFromJSONTyped,
    RouteToJSON,
} from './Route';

/**
 * 
 * @export
 * @interface Network
 */
export interface Network {
    /**
     * Created contains the timestamp when this network was created.
     * @type {Date}
     * @memberof Network
     */
    created?: Date;
    /**
     * DNSEnabled is whether name resolution is active for container on
     * this Network. Only supported with the bridge driver.
     * @type {boolean}
     * @memberof Network
     */
    dnsEnabled?: boolean;
    /**
     * Driver for this Network, e.g. bridge, macvlan...
     * @type {string}
     * @memberof Network
     */
    driver?: string;
    /**
     * ID of the Network.
     * @type {string}
     * @memberof Network
     */
    id?: string;
    /**
     * Internal is whether the Network should not have external routes
     * to public or other Networks.
     * @type {boolean}
     * @memberof Network
     */
    internal?: boolean;
    /**
     * IPAMOptions contains options used for the ip assignment.
     * @type {{ [key: string]: string; }}
     * @memberof Network
     */
    ipamOptions?: { [key: string]: string; };
    /**
     * IPv6Enabled if set to true an ipv6 subnet should be created for this net.
     * @type {boolean}
     * @memberof Network
     */
    ipv6Enabled?: boolean;
    /**
     * Labels is a set of key-value labels that have been applied to the
     * Network.
     * @type {{ [key: string]: string; }}
     * @memberof Network
     */
    labels?: { [key: string]: string; };
    /**
     * Name of the Network.
     * @type {string}
     * @memberof Network
     */
    name?: string;
    /**
     * List of custom DNS server for podman's DNS resolver at network level,
     * all the containers attached to this network will consider resolvers
     * configured at network level.
     * @type {Array<string>}
     * @memberof Network
     */
    networkDnsServers?: Array<string>;
    /**
     * NetworkInterface is the network interface name on the host.
     * @type {string}
     * @memberof Network
     */
    networkInterface?: string;
    /**
     * Options is a set of key-value options that have been applied to
     * the Network.
     * @type {{ [key: string]: string; }}
     * @memberof Network
     */
    options?: { [key: string]: string; };
    /**
     * Routes to use for this network.
     * @type {Array<Route>}
     * @memberof Network
     */
    routes?: Array<Route>;
    /**
     * Subnets to use for this network.
     * @type {Array<Subnet>}
     * @memberof Network
     */
    subnets?: Array<Subnet>;
}

/**
 * Check if a given object implements the Network interface.
 */
export function instanceOfNetwork(value: object): value is Network {
    return true;
}

export function NetworkFromJSON(json: any): Network {
    return NetworkFromJSONTyped(json, false);
}

export function NetworkFromJSONTyped(json: any, ignoreDiscriminator: boolean): Network {
    if (json == null) {
        return json;
    }
    return {
        
        'created': json['created'] == null ? undefined : (new Date(json['created'])),
        'dnsEnabled': json['dns_enabled'] == null ? undefined : json['dns_enabled'],
        'driver': json['driver'] == null ? undefined : json['driver'],
        'id': json['id'] == null ? undefined : json['id'],
        'internal': json['internal'] == null ? undefined : json['internal'],
        'ipamOptions': json['ipam_options'] == null ? undefined : json['ipam_options'],
        'ipv6Enabled': json['ipv6_enabled'] == null ? undefined : json['ipv6_enabled'],
        'labels': json['labels'] == null ? undefined : json['labels'],
        'name': json['name'] == null ? undefined : json['name'],
        'networkDnsServers': json['network_dns_servers'] == null ? undefined : json['network_dns_servers'],
        'networkInterface': json['network_interface'] == null ? undefined : json['network_interface'],
        'options': json['options'] == null ? undefined : json['options'],
        'routes': json['routes'] == null ? undefined : ((json['routes'] as Array<any>).map(RouteFromJSON)),
        'subnets': json['subnets'] == null ? undefined : ((json['subnets'] as Array<any>).map(SubnetFromJSON)),
    };
}

export function NetworkToJSON(value?: Network | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'created': value['created'] == null ? undefined : ((value['created']).toISOString()),
        'dns_enabled': value['dnsEnabled'],
        'driver': value['driver'],
        'id': value['id'],
        'internal': value['internal'],
        'ipam_options': value['ipamOptions'],
        'ipv6_enabled': value['ipv6Enabled'],
        'labels': value['labels'],
        'name': value['name'],
        'network_dns_servers': value['networkDnsServers'],
        'network_interface': value['networkInterface'],
        'options': value['options'],
        'routes': value['routes'] == null ? undefined : ((value['routes'] as Array<any>).map(RouteToJSON)),
        'subnets': value['subnets'] == null ? undefined : ((value['subnets'] as Array<any>).map(SubnetToJSON)),
    };
}

