/* tslint:disable */
/* eslint-disable */
/**
 * supports a RESTful API for the Libpod library
 * This documentation describes the Podman v2.x+ RESTful API. It consists of a Docker-compatible API and a Libpod API providing support for Podman’s unique features such as pods.  To start the service and keep it running for 5,000 seconds (-t 0 runs forever):  podman system service -t 5000 &  You can then use cURL on the socket using requests documented below.  NOTE: if you install the package podman-docker, it will create a symbolic link for /run/docker.sock to /run/podman/podman.sock  NOTE: Some fields in the API response JSON are encoded as omitempty, which means that if said field has a zero value, they will not be encoded in the API response. This is a feature to help reduce the size of the JSON responses returned via the API.  NOTE: Due to the limitations of [go-swagger](https://github.com/go-swagger/go-swagger), some field values that have a complex type show up as null in the docs as well as in the API responses. This is because the zero value for the field type is null. The field description in the docs will state what type the field is expected to be for such cases.  See podman-system-service(1) for more information.  Quick Examples:  \'podman info\'  curl --unix-socket /run/podman/podman.sock http://d/v5.0.0/libpod/info  \'podman pull quay.io/containers/podman\'  curl -XPOST --unix-socket /run/podman/podman.sock -v \'http://d/v5.0.0/images/create?fromImage=quay.io%2Fcontainers%2Fpodman\'  \'podman list images\'  curl --unix-socket /run/podman/podman.sock -v \'http://d/v5.0.0/libpod/images/json\' | jq
 *
 * The version of the OpenAPI document: 5.0.0
 * Contact: podman@lists.podman.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * ImageVolume is a volume based on a container image.  The container image is
 * first mounted on the host and is then bind-mounted into the container.  An
 * ImageVolume is always mounted read-only.
 * @export
 * @interface ImageVolume
 */
export interface ImageVolume {
    /**
     * Destination is the absolute path of the mount in the container.
     * @type {string}
     * @memberof ImageVolume
     */
    destination?: string;
    /**
     * ReadWrite sets the volume writable.
     * @type {boolean}
     * @memberof ImageVolume
     */
    readWrite?: boolean;
    /**
     * Source is the source of the image volume.  The image can be referred
     * to by name and by ID.
     * @type {string}
     * @memberof ImageVolume
     */
    source?: string;
    /**
     * SubPath mounts a particular path within the image.
     * If empty, the whole image is mounted.
     * @type {string}
     * @memberof ImageVolume
     */
    subPath?: string;
}

/**
 * Check if a given object implements the ImageVolume interface.
 */
export function instanceOfImageVolume(value: object): value is ImageVolume {
    return true;
}

export function ImageVolumeFromJSON(json: any): ImageVolume {
    return ImageVolumeFromJSONTyped(json, false);
}

export function ImageVolumeFromJSONTyped(json: any, ignoreDiscriminator: boolean): ImageVolume {
    if (json == null) {
        return json;
    }
    return {
        
        'destination': json['Destination'] == null ? undefined : json['Destination'],
        'readWrite': json['ReadWrite'] == null ? undefined : json['ReadWrite'],
        'source': json['Source'] == null ? undefined : json['Source'],
        'subPath': json['subPath'] == null ? undefined : json['subPath'],
    };
}

export function ImageVolumeToJSON(value?: ImageVolume | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'Destination': value['destination'],
        'ReadWrite': value['readWrite'],
        'Source': value['source'],
        'subPath': value['subPath'],
    };
}

